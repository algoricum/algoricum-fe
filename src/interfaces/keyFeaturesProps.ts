interface keyBenefits {
  title: string;
  subTitle: string;
}

export interface KeyFeaturesProps {
  label: string;
  title: string;
  subTitle: string;
  image: string;
  keyBenefits: keyBenefits[];
}
